{{!< default}}

<div class="letters-container">
    {{#author}}
    <header class="archive-header author-header">
        <div class="author-profile">
            {{#if profile_image}}
            <img src="{{profile_image}}" alt="{{name}}" class="author-avatar-large">
            {{else}}
            <div class="author-avatar-large avatar-placeholder">{{name.[0]}}</div>
            {{/if}}
        </div>
        <h1 class="archive-title">{{name}} 的信</h1>
        {{#if bio}}
        <p class="archive-description">{{bio}}</p>
        {{/if}}
        
        {{#if location}}
        <p class="author-location">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/>
                <circle cx="12" cy="10" r="3"/>
            </svg>
            {{location}}
        </p>
        {{/if}}
    </header>
    {{/author}}

    <section class="letters-list">
        {{#foreach posts}}
        <article class="letter-card {{post_class}}">
            <div class="letter-paper">
                <div class="letter-date">
                    <time datetime="{{date format="YYYY-MM-DD"}}">
                        {{date format="YYYY年M月D日"}}
                    </time>
                </div>
                
                <h2 class="letter-title">
                    <a href="{{url}}">{{title}}</a>
                </h2>
                
                {{#if custom_excerpt}}
                <p class="letter-excerpt">{{custom_excerpt}}</p>
                {{else}}
                <p class="letter-excerpt">{{excerpt words="60"}}</p>
                {{/if}}
                
                <a href="{{url}}" class="letter-read-more">
                    阅读全文
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M5 12h14M12 5l7 7-7 7"/>
                    </svg>
                </a>
            </div>
        </article>
        {{/foreach}}
    </section>

    {{#if pagination.pages}}
    <nav class="pagination">
        {{#if pagination.prev}}
        <a href="{{pagination.prev}}" class="pagination-prev">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M19 12H5M12 19l-7-7 7-7"/>
            </svg>
            更新的信
        </a>
        {{/if}}
        
        <span class="pagination-info">第 {{pagination.page}} 页，共 {{pagination.pages}} 页</span>
        
        {{#if pagination.next}}
        <a href="{{pagination.next}}" class="pagination-next">
            更早的信
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
        </a>
        {{/if}}
    </nav>
    {{/if}}
</div>
